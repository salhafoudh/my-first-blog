
<h1>Edit post</h1>

<%= render partial: "form", locals: {post: @post} %>
<%= button_to "Delete post", post_path, method: :delete, data: {turbo_confirm: "Are you sure, you want to delete it?"}, class:"bg-red-300 hover:bg-red-800 text-white font-bold py-2 px-4 rounded" %>